// Problem: Sentences with Functions (http://www.codewars.com/kata/52567c56cb6ac86b56000516)

function Adam(str) { 
	if (!str || !str.length ){
		str = "";
	}
	return "Adam" + str + "." ; }
function has(str) { return " has" + str; }
function a(str) { return " a " + str; }
function dog(str) { 
	if (!str || !str.length ){
		str = "";
	}	
	return "dog" + str; 
	}
function The(str) { return "The " + str; }
function name(str) { return "name " + str; }
function of(str) { return "of " + str;}
function the(str) { return "the " + str; }
function is(str) { return " is " + str;}
function also(str) { return "also " + str; }
